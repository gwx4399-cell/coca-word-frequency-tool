# Public Release Check — `coca-word-frequency-tool`

Release workspace:

```text
04_portfolio/coca-word-frequency-tool/
```

Source read-only workspace:

```text
00_sources/supporting_matierials/coca_word_tool/test-main/
```

No Git repository was initialized and nothing was published.

## Copied Files

Copied from the source project allowlist:

```text
.github/workflows/deploy-pages.yml
.gitignore
data/.gitkeep
data/COCA_WordFrequency_top5000.csv
data/README_COCA_top5050.txt
index.html
package-lock.json
package.json
src/main.tsx
src/style.css
src/vite-env.d.ts
tsconfig.json
tsconfig.node.json
vite.config.ts
```

`public/` was not copied because no `public/` directory was present in the source project.

Added or replaced for public release:

```text
LICENSE
NOTICE.md
PUBLIC_RELEASE_CHECK.md
PUBLIC_RELEASE_EXCLUSIONS.md
README.md
```

Generated by verification commands and removed after verification to keep the release workspace clean:

```text
dist/
node_modules/
```

Both are also covered by `.gitignore` if regenerated locally.

## Excluded Files

Excluded from the public-release workspace:

```text
.git/
.cursor/
.cursor/rules/coca-word-tool.mdc
README.md from the source project
hello.py
node_modules/
dist/
local caches
temporary files
task logs
resume-agent files
.env or credential files
```

Details are documented in `PUBLIC_RELEASE_EXCLUSIONS.md`.

## Sensitive-Content Findings

No API keys, tokens, passwords, private keys, `.env` files, credentials, personal contact details, or internal task links were found in the copied build files.

Public-unsafe development references were found in the source README and excluded by replacing it:

```text
git clone https://github.com/gwx4399-cell/test.git
http://localhost:5173/test/
https://gwx4399-cell.github.io/test/
.cursor/ development-rule references
```

Keyword scans also matched ordinary words inside the COCA frequency data, including `secret`, `secretary`, and `administrator`. These are vocabulary entries, not secrets.

## Public-Release Edits

- `vite.config.ts` now uses `base: '/coca-word-frequency-tool/'`.
- `package.json` and root package-lock metadata now use `coca-word-frequency-tool`.
- `README.md` was replaced with a concise bilingual public README.
- `LICENSE` was added as MIT for project code only.
- `NOTICE.md` was added for COCA-derived data attribution and redistribution cautions.
- `data/README_COCA_top5050.txt` was kept.

## Build Result

`npm ci`

```text
Result: passed
Installed 23 packages.
Audited 24 packages.
Found 0 vulnerabilities.
Warning: npm reported unknown env config "devdir".
```

`npm run build`

```text
Result: passed
Vite built successfully.
Output:
dist/index.html
dist/assets/index-C4Ehost9.css
dist/assets/index-TcMScFOd.js
```

Build warning:

```text
Some chunks are larger than 500 kB after minification.
```

Likely cause: the COCA CSV is bundled into the browser app.

## GitHub Pages Workflow

GitHub Pages workflow is present:

```text
.github/workflows/deploy-pages.yml
```

It runs:

```text
npm ci
npm run build
actions/configure-pages
actions/upload-pages-artifact
actions/deploy-pages
```

## Repository Name And Expected Pages URL

Repository name:

```text
coca-word-frequency-tool
```

Expected GitHub Pages URL:

```text
https://gwx4399-cell.github.io/coca-word-frequency-tool/
```

## Remaining Risks Before Publishing

- COCA-derived data redistribution terms should be checked against the source provider's current terms before publishing.
- The production JavaScript bundle is large because the Top 5K frequency CSV is bundled into the client app.
- `node_modules/` and `dist/` were generated during verification and then removed; they are ignored by `.gitignore` if regenerated later.
- The app uses surface-form matching with a limited tokenizer and no lemmatization; README scope and limitations disclose this.
